# 開発指示：MemoPolish のピボット

現在のプロジェクトは Gemini API を使った文章のAI加工ツールとして動いています。  
これを **デバイス間でフレーズを共有する「PhraseBridge」** にピボットします。

AI 加工機能は残しますが、**必須ではなく任意**にします。

---

## 目的（MVP）

PC とスマホ間で、手間なくフレーズ共有ができること。  
Gmail の下書きよりも **簡単・速い** 体験にすること。

**PhraseBridge + 任意の AI 加工** がプロダクトの姿です。

---

## コア機能の変更

### 1. フレーズ保存（Supabase）

フレーズを永続保存できるようにする。

`phrases` テーブルを作成：

| column         | type        |
|----------------|-------------|
| id             | uuid (PK)   |
| bucket_id      | text        |
| text           | text        |
| write_key_hash | text        |
| created_at     | timestamptz |
| expires_at     | timestamptz |

---

### 2. bucket_id の考え方（重要）

ユーザーセッションごとに `bucket_id` を持つ。

- 初回アクセス時にランダム生成（22文字以上）
- 生成方法は `crypto.randomUUID()` または `crypto.getRandomValues` ベース
- localStorage に保存
- UI に `bucket_id` を表示し、コピー可能にする
- 手動入力で `bucket_id` を切り替え可能（デバイス間同期用）
- `bucket_id` は「閲覧キー」扱いにする（共有は自己責任）

---

### 3. 必須の新機能

1. Supabase へフレーズ保存
2. `bucket_id` でフレーズ一覧取得（最新順）
3. ワンタップでコピー
4. フレーズ削除
5. 期限切れフレーズは自動で整理

---

### 4. write_key の考え方（削除を安全にする）

認証なしで削除を許可する場合は、**書き込み/削除用の鍵**を分けて持つ。

- `bucket_id` は閲覧キー
- `write_key` は追加・削除キー
- 初回に `write_key` を生成して localStorage に保存
- Supabase には `write_key_hash` を保存
- 追加/削除時は `write_key` を添えて一致した場合のみ許可
- 同じ `bucket_id` を複数端末で使う場合は **同じ write_key を共有**する

MVPで最小限の安全性を確保するための前提とする。

---

## AI 加工（任意）

既存の `/api/polish` は維持する。

UI に以下の 2 ボタンを追加：

- 「AI加工してコピー」
- 「AI加工して保存」

通常の保存は **AI なしで完結**すること。

---

## UI 変更

画面は 1 画面構成。

上部：
- テキスト入力

ボタン：
- 保存
- AI加工してコピー
- AI加工して保存

中央：
- 共有ルームID（内部名: `bucket_id`）表示 + コピー

編集キー（詳細）：
- 編集キー（内部名: `write_key`）表示 + コピー
- `write_key` 手動入力 + 適用

下部：
- フレーズ一覧
  - コピー
  - 削除
  - AI加工

---

## 技術要件

- フロントエンドで Supabase クライアントを使用
- 認証は不要（MVP）
- Gemini API の使い方は変更しない
- 既存のAI加工ロジックは削除しない
- Supabase の RLS を有効化（MVP）
- RLS 方針（MVP割り切り）：
  - 読み取り：`bucket_id` が一致する行のみ
  - 書き込み：`bucket_id` と `write_key_hash` が一致する行のみ
- 削除・cleanup はサーバAPIで実行（service role key 使用）

---

## 制約

- コードはできるだけシンプルに
- 認証機能は追加しない
- 複雑な状態管理ライブラリは使わない
- モバイルで問題なく動作すること
- 文字数上限（例：2000文字）
- 1 bucket あたりの保持数上限（200件）
- 追加時に上限超過なら古いものを削除
- UI 連打防止（多重送信の抑制）

---

## 完了条件（Definition of Done）

- 同じ `bucket_id` を持つ 2 台の端末で同じ一覧が見える
- 同じ `bucket_id` で同じ `write_key` を共有できる
- コピーはワンタップでできる
- AI を使わず保存できる
- AI加工は要求時に動作する

---

## 実装上の補足（MVPの割り切り）

- RLSは有効化するが、MVPでは「削除・追加」の保護を主目的とする。  
  閲覧は推測困難な bucket_id（22文字以上）＋ TTL ＋ 件数上限によりリスクを許容する。
- expires_at はMVPでは「期限切れは非表示」を基本とし、cleanupで削除する。
- write_key_hash は SHA-256（Web Crypto）で生成し、DBには平文の write_key を保存しない。

---

## UI体験改善（操作フィードバック）

PhraseBridgeは日常的に使う軽量ツールであるため、  
「操作したことが視覚的に伝わること」を重要なUX要件とする。

### 1. ボタン押下中の状態表示

非同期処理中は、対象操作のボタンを明確に変化させる。

対象操作:

- 保存
- AI加工してコピー
- AI加工して保存
- 削除

表示仕様:

- 実行中は `disabled`
- `opacity` を下げる（例: 0.6）
- ボタン内に小さなスピナーを表示
- 二重送信を防止する
- 操作単位で `busy` を持ち、必要以上に他操作をブロックしない

### 2. 成功時トースト通知

成功時に、画面右下へ一時表示のトーストを出す。

表示文:

| 操作 | 表示文 |
| --- | --- |
| 保存 | 保存しました |
| コピー | コピーしました |
| 削除 | 削除しました |
| AI加工保存 | AI加工して保存しました |

表示仕様:

- 2-3秒で自動消滅
- Tailwindで軽量実装（外部ライブラリ不要）
- 画面最上位レイヤーに固定表示
- フォーム上部の成功メッセージは併用または置き換え方針を明記する

### 3. 削除アニメーション

削除時の視覚フィードバックを追加する。

- クリック直後に対象行をフェードアウト
- 削除成功後にリストから除去
- 失敗時は表示を戻してエラー通知
- アニメーション時間は 150-250ms を目安に統一する

### 4. APIエラー表示の視覚強化

エラー発生時:

- トーストで赤色通知表示
- フォーム上部のエラーテキストと併用可
- トーストは補助、フォーム上部のエラーは主表示とする

### 5. UXの目的

これらは機能追加ではなく、  
「反応のないWebフォーム感」をなくし、常用できるツール体験にするための改善である。

### 6. 入力欄の状態表示（重要）

入力中のテキストが「未保存」かどうかを視覚的に示す。
仕様：
入力変更後、未保存状態になる
保存成功時に「保存済み」状態へ
未保存時は入力欄下に小さく「未保存」と表示（薄い色）
保存済み時は「保存済み」と表示（緑系）
これにより「保存押したか忘れた問題」を解消する
